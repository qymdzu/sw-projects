ai_routing:
  default: deepseek
  models:
    chat: deepseek-chat
    reasoner: deepseek-reasoner
  rules:
    - when: { purpose: "tagging" }
      use: deepseek
    - when: { purpose: "variant" }
      use: deepseek
    - when: { purpose: "knowledge" }
      use: deepseek
