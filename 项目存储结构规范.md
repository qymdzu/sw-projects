# 项目存储结构规范

> 版本：v2.0.0  
> 适用范围：sw-projects 下所有软件开发项目  
> 更新日期：2026-06-30  
> 核心变更：新增过程产出（pdca/）子目录体系，新增整合产出机制

---

## 0. 通用开发原则

> **所有 sw-projects 下项目一律遵守**：

1. **架构一次性搭好** —— Stage 3 系统设计阶段**必须**把**所有**功能需求纳入架构考虑，输出完整架构（架构设计 / 数据模型 / API / 目录结构），后续版本迭代**不返工改架构**。
2. **代码分批做** —— v0.1 / v0.2 / v0.3 按 MVP 范围分批实现，每批跑通后再加下一批功能。
3. **过程即产物** —— 每个阶段的子 PDCA 中间产出必须存储到对应 `pdca/` 目录，不跳过、不合并。
4. **整合必有校验** —— 过程产出整合为最终文档前，必须经过交叉引用检查和一致性校验。
5. **硬编码锁死目录** —— 项目骨架由 `project_init()` 创建，Agent 不创建目录、不走偏。

---

## 一、总则

1. 所有软件开发项目必须遵循本规范创建目录结构。
2. 项目启动时**必须先确定项目形态**，根据形态确定保留哪些目录，不保留的目录直接删除。
3. **保留即受检**：凡保留的目录，在 Pipeline 对应阶段必须通过门禁校验，不得为空或缺失关键文件。
4. **过程目录不可删除**：`pdca/` 及其子目录为过程产物，即使最终交付物已生成也必须保留（供审计和追溯）。
5. 本规范文件位于 `sw-projects/` 根目录，每个项目创建时参照执行。

---

## 二、项目形态定义

项目启动时从以下形态中选择一种：

| 形态 | 说明 | 保留目录 |
|:-----|:-----|:---------|
| **fullstack** | 全栈项目（后端 + 前端） | 全部目录 |
| **backend-only** | 纯后端项目 | 去掉 `frontend/`、`ai/` |
| **frontend-only** | 纯前端项目 | 去掉 `backend/`、`ai/` |
| **tool-script** | 工具脚本/小工具 | 仅保留 `scripts/`、`docs/`、`README.md` |
| **ai-service** | AI 服务项目 | 去掉 `frontend/`，保留 `ai/` + `backend/` |

项目根目录必须存放 `.project-meta.yaml` 文件记录形态定义：

```yaml
# .project-meta.yaml
project_name: "项目名称"
project_type: "fullstack"
version: "0.1.0"
created_at: "2026-06-30"
pipeline_stages: [1,2,3,4,5,6,7,8]
scope_v0_1: ["FR-001", "FR-002"]
architecture_full: 30
```

---

## 三、标准目录结构（v2.0）

### 3.1 目录总览

```
{project_name}/
├── README.md
├── CHANGELOG.md
├── .gitignore
├── LICENSE
├── .project-meta.yaml
│
├── docs/
│   ├── requirements/          ← Stage 1：需求分析
│   │   ├── 需求规格说明书.md   ← 最终整合产出
│   │   ├── 待确认问题.md
│   │   └── pdca/              ← 过程产出：8 维度子 PDCA
│   │       ├── 6.1-商业可行性/
│   │       │   ├── analysis.md
│   │       │   └── pdca-result.md
│   │       ├── 6.2-用户需求/
│   │       ├── ...（同结构，逐维度）
│   │       └── 6.8-术语与假设/
│   │
│   ├── research/              ← Stage 2：技术调研
│   │   ├── 技术方案说明书.md    ← 最终整合产出
│   │   ├── 技术调研清单.md
│   │   └── pdca/              ← 过程产出：按技术层分开调研
│   │       ├── backend/
│   │       ├── database/
│   │       ├── frontend/
│   │       ├── ai/
│   │       ├── devops/
│   │       └── thirdparty/
│   │
│   ├── design/                ← Stage 3：系统设计
│   │   ├── 系统架构设计.md     ← 最终整合产出
│   │   ├── 数据模型设计.md
│   │   ├── API设计.md
│   │   ├── 目录结构.md
│   │   └── pdca/              ← 过程产出：D1~D4 逐模块
│   │       ├── modules/       ← D1：每模块一个子目录
│   │       │   ├── {module-a}/
│   │       │   │   ├── design.md
│   │       │   │   └── pdca-result.md
│   │       │   ├── {module-b}/
│   │       │   └── ...
│   │       ├── data-model/    ← D2
│   │       ├── api-design/    ← D3
│   │       └── directory/     ← D4
│   │
│   ├── ux/                    ← Stage 4：UI/UX 设计
│   │   ├── 交互流程设计.md     ← 最终整合产出
│   │   ├── UI组件规范.md
│   │   ├── 视觉风格指南.md
│   │   └── pdca/              ← 过程产出：每角色 + 每组件
│   │       ├── role-{name}/
│   │       │   ├── flow.md
│   │       │   └── pdca-result.md
│   │       ├── comp-{name}/
│   │       │   ├── spec.md
│   │       │   └── pdca-result.md
│   │       └── ...
│   │
│   ├── review/                ← Stage 6：代码审查
│   │   ├── 代码审查报告.md     ← 最终整合产出
│   │   └── pdca/              ← 过程产出：P0/P1/P2 逐级审查
│   │       ├── p0-blocking/
│   │       │   └── pdca-result.md
│   │       ├── p1-warning/
│   │       │   └── pdca-result.md
│   │       └── p2-suggestion/
│   │           └── pdca-result.md
│   │
│   ├── test/                  ← Stage 7：全量测试
│   │   ├── 测试计划.md         ← 最终整合产出
│   │   ├── 测试报告.md
│   │   └── pdca/              ← 过程产出：每模块独立测试
│   │       ├── module-{a}/
│   │       │   ├── test-cases.md
│   │       │   └── pdca-result.md
│   │       ├── module-{b}/
│   │       └── ...
│   │
│   └── deploy/                ← Stage 8：部署交付
│       ├── 部署手册.md         ← 最终整合产出
│       └── pdca/              ← 过程产出：逐组件部署设计
│           ├── docker/
│           ├── scripts/
│           ├── config/
│           └── cicd/
│
├── pseudocode/                ← Stage 5 编码之 Phase A：伪代码
│   ├── index.md               ← 最终整合产出：全部伪代码的索引 + 交叉引用图
│   └── {module-a}/            ← 按模块分，与 arch 输出的架构模块一一对应
│   │   ├── pseudocode.md
│   │   └── pdca-result.md
│   ├── {module-b}/
│   └── ...
│
├── backend/                   ← Stage 5 编码之 Phase B：真实代码（Go）
│   ├── cmd/server/main.go
│   ├── internal/
│   │   ├── handler/
│   │   ├── service/
│   │   │   ├── auth.go        ← 引用 pseudocode/{module-auth}/
│   │   │   └── ...
│   │   ├── repository/
│   │   ├── model/
│   │   ├── middleware/
│   │   └── config/
│   ├── pkg/
│   │   ├── response/
│   │   └── errors/
│   ├── migrations/
│   ├── scripts/
│   ├── go.mod
│   └── go.sum
│
├── frontend/                  ← 有前端时保留
│   ├── src/
│   ├── public/
│   ├── config/
│   ├── scripts/
│   └── package.json
│
├── ai/                        ← 有 AI 组件时保留
│   ├── scripts/
│   ├── notebooks/
│   └── requirements.txt
│
├── deploy/                    ← 部署配置（Stage 8 编辑）
│   ├── docker/
│   │   ├── Dockerfile
│   │   ├── Dockerfile.frontend
│   │   └── docker-compose.yml
│   ├── config/
│   │   ├── development/
│   │   ├── staging/
│   │   └── production/
│   └── scripts/
│       └── deploy.sh
│
├── scripts/                   ← 通用工具脚本
│   ├── build.sh
│   ├── test.sh
│   ├── setup.sh
│   └── lint.sh
│
├── tests/                     ← 全量测试
│   ├── unit/
│   ├── integration/
│   └── e2e/
│
└── .github/
    └── workflows/
        └── deploy.yml
```

---

## 四、每阶段：过程产出 → 整合产出的对应关系

### 核心模型

```
┌──────────────────────────────┐
│  子 PDCA 1（pdca/{dim}/）     │
│  子 PDCA 2（pdca/{dim}/）     │  ← 过程产出：逐模块/逐维度
│  ...                          │     独立 PDCA、独立评分
│  子 PDCA N（pdca/{dim}/）     │
└───────────────┬──────────────┘
                │
                ▼
┌──────────────────────────────┐
│  整合（Integration）          │  ← Agent 自己的最后一步
│  • 交叉引用检查               │    检查：N→M 的引用关系
│  • 一致性校验                 │    矛盾检测
│  • 关联性验证                 │    A 说用 MySQL，B 说用 PG → 冲突
│  • 格式统一                   │
└───────────────┬──────────────┘
                │
                ▼
┌──────────────────────────────┐
│  最终交付物（README.md /      │  ← 公子看到的产出
│  需求规格说明书.md / ...）     │
└──────────────────────────────┘
```

### 逐阶段对应表

| 阶段 | 过程产出目录 | 过程内容 | 整合产出文件 | 整合校验内容 |
|:-----|:------------|:---------|:-------------|:------------|
| **1 需求分析** | `docs/requirements/pdca/{6.1~6.8}/` | 8 维度各一份 analysis + pdca-result | `需求规格说明书.md` | 8 维矛盾检测、P0 标注一致性、角色定义重叠检查 |
| **2 技术调研** | `docs/research/pdca/{backend,database,...}/` | 6 层技术栈各一份选型对比 | `技术方案说明书.md` + `技术调研清单.md` | 层间兼容性（后端选 Go → DB 驱动必须有 Go 客户端） |
| **3 系统设计** | `docs/design/pdca/{modules/*,data-model,api-design,directory}/` | D1 每模块设计、D2 数据模型、D3 API、D4 目录 | 4 份设计文档 | 模块间引用关系、API 实体与数据模型实体一致、目录与模块列表一致 |
| **4 UI/UX** | `docs/ux/pdca/{role-*,comp-*}/` | 每角色任务流 + 每组件规范 | 3 份 UX 文档 | 角色流程不矛盾、组件状态完整 |
| **5 编码** | `pseudocode/{module}/`（Phase A） | 每模块伪代码 + PDCA | `pseudocode/index.md`（索引 + 交叉引用图） | 伪代码引用 API 存在性、模块间调用链完整 |
| **6 审查** | `docs/review/pdca/{p0,p1,p2}/` | P0/P1/P2 逐级审查记录 | `代码审查报告.md` | 所有 P0 已关闭、P1 有跟踪 |
| **7 测试** | `docs/test/pdca/{module-*}/` | 每模块测试用例 + 执行结果 | `测试计划.md` + `测试报告.md` | 模块测试覆盖率汇总、Bug 关联 |
| **8 部署** | `docs/deploy/pdca/{docker,scripts,config,cicd}/` | 逐组件部署设计 | `部署手册.md` | 整体部署链端到端验证 |

---

## 五、pdca-result.md 统一格式

过程产出的每个子 PDCA 必须附带 `pdca-result.md`，格式如下：

```markdown
# PDCA 评分：{维度/模块名称}

## Plan（计划）
- 目标：{一句话目标}
- 步骤：{N} 步计划
- 评分：/5

## Do（执行）
- 产出：{产出物清单}
- 评分：/5

## Check（自检）
- 自检项：{N} 项
- 通过：{N} 项
- 失败：{N} 项
- 评分：/5

## Action（改进）
- 发现问题：{N} 个
- 已修复：{N} 个
- 遗留：{N} 个（如有，附理由）
- 评分：/5

## 汇总
| 维度 | Plan | Do | Check | Action | 总分 |
|:-----|:----:|:--:|:-----:|:------:|:----:|
| 评分 | /5 | /5 | /5 | /5 | /20 |

**门禁**：每项 ≥3/5，总分 ≥80%。
```

---

## 六、整合步骤模板（每 Agent 通用）

每个 Agent 在完成过程产出后，必须执行以下整合步骤：

### Step 1：交叉引用检查
- 列出本阶段所有子 PDCA 之间的引用关系
- 检查被引用方是否存在、内容是否匹配
- 标记引用断裂处

### Step 2：一致性校验
- 遍历所有子 PDCA 的结论
- 检查矛盾（如：模块 A 说数据在 MySQL，模块 B 说在 PostgreSQL）
- 标记矛盾处

### Step 3：整合输出
- 将一致且无矛盾的过程产出汇总为最终文档
- 格式按调令模板严格固定

### Step 4：整合后 PDCA
- 对最终整合文档做一次 PDCA 评分
- 写入 `pdca-result.md`（最终文件同级目录）

```markdown
## 整合后 PDCA

| 维度 | Plan | Do | Check | Action | 总分 |
|:-----|:----:|:--:|:-----:|:------:|:----:|
| 整合 | /5 | /5 | /5 | /5 | /20 |
```

**门禁**：每项 ≥3/5，总分 ≥80%，且交叉引用检查 0 断裂、一致性检查 0 矛盾。

---

## 七、项目初始化流程

### 步骤 1：确定项目形态

公子在飞书发布开发需求时同步指定项目形态：
> "新建一个用户管理系统，fullstack"

如未指定，默认 **backend-only**。

### 步骤 2：Runner 执行 project_init()

`pipeline-runner.py` 的 `project_init()` 函数自动执行（零 LLM 参与）：

1. 根据 project_type 确定完整目录列表
2. `os.makedirs` 创建所有目录
3. `Path.touch` 创建所有空文件（含 `pdca/` 子目录中的所有占位文件）
4. 写入 `.project-meta.yaml`（固定模板）
5. 写入 `.gitignore`（Go 综合模板）
6. 写入 `README.md`（模板填充）
7. 写入 `CHANGELOG.md`（空模板）
8. 各子目录写入 `README.md`（一行职责说明）
9. `git add + git commit + git push` 到 Gitee `sw-projects` 仓库

**红线**：Agent 不得创建目录，不得删除骨架文件，不得修改骨架文件名。

### 步骤 3：推送至 Gitee

骨架创建完成并推送 `sw-projects/<project-name>/` 后，正式启动 Pipeline。

---

## 八、门禁校验规则（保留即受检）

### 核心原则

- 凡骨架中保留的目录，在 Pipeline 对应阶段结束时必须通过门禁校验
- 过程目录（`pdca/`）即使不是最终交付物，也必须经过 Runner 的 PDCA 校验

### 逐目录门禁

| 目录 | Stage | 过程门禁（pdca/） | 最终门禁（交付物） |
|:-----|:------|:-----------------|:-----------------|
| `docs/requirements/` | 1 | 全部 8 维 PDCA ≥3/5，总分 ≥80% | `需求规格说明书.md` 存在、P0 标注、MVP 范围 |
| `docs/research/` | 2 | 各技术层 PDCA 通过 | `技术方案说明书.md` 存在、≥2 候选对比 |
| `docs/design/` | 3 | D1~D4 各 PDCA 通过 | 4 份设计文档存在且非空 |
| `docs/ux/` | 4 | 各角色+组件 PDCA 通过 | 3 份 UX 文档存在、无框架绑定 |
| `pseudocode/` | 5A | 每模块 PDCA + 交叉引用 0 断裂 | `index.md` 完整索引 |
| `backend/` | 5B | —（真实代码不额外 PDCA） | `go build` + `go test` 通过 |
| `docs/review/` | 6 | P0/P1/P2 审查 PDCA | `代码审查报告.md` 存在、P0 已关闭 |
| `docs/test/` | 7 | 每模块测试 PDCA | `测试报告.md` 存在、结论明确 |
| `docs/deploy/` | 8 | 逐组件部署 PDCA | `部署手册.md` 7 章完整 |

### 整合后补充门禁

| 校验项 | 方法 | 说明 |
|:-------|:-----|:-----|
| 交叉引用断裂 | `check_gate.py --type check_cross_ref` | 阶段内所有引用关系检查 |
| 一致性矛盾 | `check_gate.py --type check_consistency` | 阶段内矛盾检测 |
| 整合 PDCA | `check_gate.py --type check_pdca_result` | 最终文档 PDCA ≥80% |

---

## 九、版本规范

### 语义化版本

```
MAJOR.MINOR.PATCH
```

| 版本 | 触发条件 |
|:-----|:---------|
| MAJOR | 不兼容的目录或文件变更 |
| MINOR | 新增可选目录/文件，向后兼容 |
| PATCH | 描述修正、规范细节补充 |

---

## 十、附录

### A. pdca-result.md 示例

```markdown
# PDCA 评分：6.3 功能需求

## Plan（4/5）
- 目标：完整列出所有 P0/P1/P2/P3 功能需求
- 步骤：4 步（捕获→分类→优先级→描述）
- 扣分理由：P2 功能缺少与 P0 的关联性分析

## Do（5/5）
- 产出：功能清单表 + P0 详细描述
- 评分：全部完成

## Check（4/5）
- 自检项：6 项（存在性、完整性、一致性、优先级、角色覆盖、边界）
- 通过：5 项 | 失败：1 项
- 扣分理由：P0 功能 F-003 缺少边界条件描述

## Action（3/5）
- 发现问题：2 个
- 已修复：1 个 | 遗留：1 个
- 扣分理由：F-003 边界条件需公子确认

## 汇总
| Plan | Do | Check | Action | 总分 |
|:----:|:--:|:-----:|:------:|:----:|
| 4/5 | 5/5 | 4/5 | 3/5 | 16/20 |
```

### B. 交叉引用检查示例

```markdown
# 交叉引用检查：需求分析阶段

## 维度间引用
| 引用方 | 被引用方 | 状态 |
|:-------|:---------|:----:|
| 6.3 功能需求 → F-001 | 6.2 用户需求 → 老师角色 | ✅ 匹配 |
| 6.3 功能需求 → F-002 | 6.4 非功能需求 → 响应时间 | ✅ 匹配 |
| 6.3 功能需求 → F-005 | 6.5 数据需求 → 试卷数据 | ❌ 断裂：6.5 未定义试卷数据结构 |

## 矛盾检测
| 声明 A | 声明 B | 矛盾？ |
|:-------|:-------|:------:|
| 6.3 说 10 个 P0 功能 | 6.1 说 MVP 只有 8 个 | ⚠️ 数量不一致 |
| 6.4 说支持 1000 并发 | 6.5 说单机部署 | ⚠️ 容量与架构矛盾 |

## 结论
- 引用断裂：1 处（需修复）
- 矛盾：2 处（需修复）
- **门禁：0 断裂、0 矛盾 方可整合**
```

### C. 项目快速创建参考

```bash
# 完整 fullstack 骨架（v2.0）
# 实际由 project_init() 自动执行，此命令仅作参考
mkdir -p {project}/{docs/{requirements/pdca/{6.1-商业可行性,6.2-用户需求,6.3-功能需求,6.4-非功能需求,6.5-数据需求,6.6-集成需求,6.7-安全与合规,6.8-术语与假设},research/pdca/{backend,database,frontend,ai,devops,thirdparty},design/pdca/{modules/{mod1,mod2,...},data-model,api-design,directory},ux/pdca/{role-admin,role-user,comp-login,comp-dashboard},review/pdca/{p0-blocking,p1-warning,p2-suggestion},test/pdca/{module-a,module-b},deploy/pdca/{docker,scripts,config,cicd}},\
pseudocode/{mod1,mod2,...},\
backend/{cmd/server,internal/{handler,service,repository,model,middleware,config},pkg/{response,errors},migrations,scripts},\
frontend/{src,public,config,scripts},\
ai/{scripts,notebooks},\
deploy/{docker,config/{development,staging,production},scripts},\
scripts,\
tests/{unit,integration,e2e},\
.github/workflows}
```