# 项目存储结构规范

> 版本：v1.0.0  
> 适用范围：sw-projects 下所有软件开发项目  
> 更新日期：2026-06-03

---

## 0. 通用开发原则（2026-06-06 公子拍板，全项目适用）

> **所有 sw-projects 下项目一律遵守**：

1. **架构一次性搭好** —— Stage 3 系统设计阶段**必须**把**所有**功能需求纳入架构考虑，输出完整架构（架构设计 / 数据模型 / API / 目录结构），后续版本迭代**不返工改架构**。
2. **代码分批做** —— v0.1 / v0.2 / v0.3 按 MVP 范围分批实现，每批跑通后再加下一批功能。

**反模式**：
- ❌ "v0.1 只做一半需求 → 架构先做一半 → v0.2 加新功能时改架构"
- ❌ "架构跟着 MVP 范围走，30 条需求只考虑 12 条"

**正确做法**：
- ✅ Stage 3 一次性把 30 条全考虑完（架构文档 100% 覆盖）
- ✅ Stage 5 编码 v0.1 只实现 12 条 P0（代码 40% 覆盖）
- ✅ v0.2 加 18 条功能 = 加代码，**不改架构**

**项目元信息记录**（`.project-meta.yaml`）：
- `scope_v0_1`: v0.1 实现的 P0 需求 ID 列表（如 `[FR-001, FR-002, ...]`）
- `architecture_full`: 架构设计的总需求数（30）  
- 两者比值 = 当前实现进度，不影响架构完整度

---

## 一、总则

1. 所有软件开发项目必须遵循本规范创建目录结构。
2. 项目启动时**必须先确定项目形态**，根据形态确定保留哪些目录，不保留的目录直接删除。
3. **保留即受检**：凡保留的目录，在 Pipeline 对应阶段必须通过门禁校验，不得为空或缺失关键文件。
4. 本规范文件位于 `sw-projects/` 根目录，每个项目创建时参照执行。

---

## 二、项目形态定义

项目启动时从以下形态中选择一种：

| 形态 | 说明 | 保留目录 |
|:-----|:-----|:---------|
| **fullstack** | 全栈项目（后端 + 前端） | 全部目录 |
| **backend-only** | 纯后端项目 | 去掉 `frontend/`、`ai/` |
| **frontend-only** | 纯前端项目 | 去掉 `backend/`、`ai/` |
| **tool-script** | 工具脚本/小工具 | 仅保留 `scripts/`、`docs/`、`README.md` |
| **ai-service** | AI 服务项目 | 去掉 `frontend/`，保留 `ai/` + `backend/` |

项目根目录必须存放 `.project-meta.yaml` 文件记录形态定义：

```yaml
# .project-meta.yaml
project_name: "项目名称"
project_type: "fullstack"          # fullstack | backend-only | frontend-only | tool-script | ai-service
version: "0.1.0"
created_at: "2026-06-03"
pipeline_stages: [1,2,3,4,5,6,7,8]  # 本项目实际执行的 Pipeline 阶段编号
```

---

## 三、标准目录结构

### 3.1 根目录文件

```
project-name/
├── README.md                 ← 项目说明（技术栈、快速启动、目录导读）
├── CHANGELOG.md              ← 版本变更记录（语义化版本格式）
├── .gitignore                ← Git 忽略规则（Go/Vue/Python 综合模板）
├── LICENSE                   ← 许可证文件（有发布需求时填充）
└── .project-meta.yaml        ← 项目元信息
```

### 3.2 Pipeline 产物目录 `docs/`

```
docs/
├── requirements/             ← Stage 1：需求分析
│   ├── 需求规格说明书.md     ← 必须
│   └── 待确认问题.md         ← 有则必存
├── research/                 ← Stage 2：技术调研
│   ├── 技术方案说明书.md     ← 必须
│   └── 技术调研清单.md       ← 必须
├── design/                   ← Stage 3：系统设计
│   ├── 系统架构设计.md       ← 必须
│   ├── 数据模型设计.md       ← 必须
│   ├── API设计.md            ← 必须
│   └── 目录结构.md           ← 必须
├── ux/                       ← Stage 4：UI/UX 设计
│   ├── 交互流程设计.md        ← 必选
│   ├── UI组件规范.md          ← 必选
│   └── 视觉风格指南.md        ← 可选（纯后端项目跳过）
├── review/                   ← Stage 6：审查
│   ├── 代码审查报告.md       ← 编码完成后必须
│   └── 审查意见汇总.md       ← 可选
├── test/                     ← Stage 7：测试
│   ├── 测试计划.md           ← 必须
│   └── 测试报告.md           ← 必须
└── deploy/                   ← Stage 8：部署
    ├── 部署手册.md           ← 必须
    └── 运维说明.md           ← 可选
```

### 3.3 后端代码目录 `backend/`（Go）

```
backend/
├── cmd/server/main.go        ← 应用入口
├── internal/
│   ├── handler/              ← HTTP 处理器
│   ├── service/              ← 业务逻辑
│   ├── repository/           ← 数据访问层
│   ├── model/                ← 数据模型定义
│   ├── middleware/            ← 中间件（认证/日志/限流）
│   └── config/               ← 配置加载
├── pkg/                      ← 可导出公共库
│   ├── response/             ← 统一响应格式
│   └── errors/               ← 统一错误码
├── migrations/               ← 数据库迁移脚本
├── scripts/                  ← 后端工具脚本
├── go.mod
└── go.sum
```

### 3.4 前端代码目录 `frontend/`（Vue）

```
frontend/
├── src/
├── public/
├── config/
├── scripts/
└── package.json
```

### 3.5 AI 辅助模块 `ai/`（Python）

```
ai/
├── scripts/
├── notebooks/
└── requirements.txt
```

### 3.6 部署配置 `deploy/`

```
deploy/
├── docker/
│   ├── Dockerfile.backend
│   ├── Dockerfile.frontend     ← 有前端时保留
│   └── docker-compose.yml
├── config/
│   ├── development/
│   ├── staging/
│   └── production/
└── scripts/
```

### 3.7 通用脚本 `scripts/`

```
scripts/
├── build.sh
├── test.sh
├── setup.sh
└── lint.sh
```

### 3.8 全量测试 `tests/`

```
tests/
├── unit/
├── integration/
└── e2e/                      ← 有前端时保留
```

---

## 四、项目初始化流程

### 步骤 1：确定项目形态

公子在飞书发布开发需求时同步指定项目形态：
> "新建一个用户管理系统，fullstack"

如未指定，默认 **backend-only**。

### 步骤 2：创建目录骨架

翠花根据项目形态保留对应目录，删除不必要的目录。

| 项目形态 | 保留规则 |
|:---------|:---------|
| `fullstack` | 全部保留 |
| `backend-only` | 删除 `frontend/`、`ai/`、`tests/e2e/` |
| `frontend-only` | 删除 `backend/`、`ai/`、`deploy/docker/Dockerfile.backend` |
| `tool-script` | 仅保留 `scripts/`、`docs/`、`README.md` |
| `ai-service` | 删除 `frontend/`、`tests/e2e/` |

### 步骤 3：推送至 Gitee

骨架创建完成并推送 `sw-projects/<project-name>/` 后，正式启动 Pipeline。

---

## 五、门禁校验规则（保留即受检）

**核心原则**：凡骨架中保留的目录，在 Pipeline 对应阶段结束时必须通过门禁校验。

| 目录 | 所属 Stage | 门禁条件 |
|:-----|:-----------|:---------|
| `docs/requirements/` | 1 | `需求规格说明书.md` 存在且 self_check 全 PASS |
| `docs/research/` | 2 | `技术方案说明书.md` + `技术调研清单.md` 存在，选型有 ≥2 候选对比 |
| `docs/design/` | 3 | 4 个设计文档全部存在且非空，技术选型与 Stage 2 一致 |
| `docs/ux/` | 4 | 仅当保留 `frontend/` 时校验：`界面原型.md` + `交互流程.md` 存在 |
| `backend/` | 5 | 仅当保留 `backend/` 时校验：`cmd/server/main.go` 存在，项目可编译 |
| `frontend/` | 5 | 仅当保留 `frontend/` 时校验：`package.json` 存在，项目可构建 |
| `ai/` | 5 | 仅当保留 `ai/` 时校验：`requirements.txt` 存在 |
| `docs/review/` | 6 | `代码审查报告.md` 存在，审查意见已处理 |
| `docs/test/` | 7 | `测试计划.md` + `测试报告.md` 存在，测试覆盖率达标 |
| `deploy/` | 8 | `deploy/docker/docker-compose.yml` 存在，配置可启动 |

### 校验失败处理

| 情况 | 处理方式 |
|:-----|:---------|
| 文件不存在 | Pipeline 阻塞，翠花通知公子并说明缺失文件 |
| 文件内容空 | 退回对应 Agent 补充内容 |
| 校验不通过 | 翠花给出具体失败原因，公子决定修复或降级通过 |
| 目录被删除但门禁触发 | 说明该目录已被排除，跳过校验 |

---

## 六、版本规范

### 语义化版本

本规范采用 [Semantic Versioning 2.0.0](https://semver.org/)：

```
MAJOR.MINOR.PATCH
```

| 版本号 | 触发条件 |
|:-------|:---------|
| MAJOR | 项目存储结构发生不兼容的目录或文件变更 |
| MINOR | 新增可选目录或文件，向后兼容 |
| PATCH | 修正描述、补充规范细节 |

### CHANGELOG 格式

```markdown
# Changelog

## [0.1.0] - 2026-06-03

### Added
- 项目初始化，基础框架搭建
```

---

## 七、附录

### A. 项目快速创建命令

```bash
# 在 sw-projects 根目录下执行
mkdir -p <project-name>/{docs/{requirements,research,design,ux,review,test,deploy},backend/{cmd/server,internal/{handler,service,repository,model,middleware,config},pkg/{response,errors},migrations,scripts},frontend/{src,public,config,scripts},ai/{scripts,notebooks},deploy/{docker,config/{development,staging,production},scripts},scripts,tests/{unit,integration,e2e}}
touch <project-name>/README.md
touch <project-name>/CHANGELOG.md
touch <project-name>/.gitignore
touch <project-name>/.project-meta.yaml
```

### B. 模板文件引用

各阶段文档模板位于 `sw-agents-workspace/<角色>/references/` 下：
- Stage 1~2 模板 → `analyst/references/`
- Stage 3 模板 → `arch/references/`
- Stage 4~8 模板（待定义）
